import React from "react";
import styled from "styled-components";

const Wrapper = styled("header")`
    color: blueviolet;
`;

export const Header: React.StatelessComponent = () => <Wrapper>header</Wrapper>;
